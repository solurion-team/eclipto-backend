server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: user
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true

auth:
  jwks-kid: 5bd61546-a85a-4d6a-8113-9e6c6f4425dd
  jwt:
    issuer: http://localhost:8080
    period:
      days: 7
      hours: 10

logging:
  level:
    org:
      springframework:
        security:
          oauth2: DEBUG